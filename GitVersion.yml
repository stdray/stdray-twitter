mode: ContinuousDelivery
branches:
  main:
    regex: ^master$|^main$
    mode: ContinuousDelivery
    tag: ""
    increment: Patch
    is-source-branch-for: ['*']
  develop:
    regex: ^dev(elop)?(ment)?$
    mode: ContinuousDeployment
    tag: 'alpha'
    increment: Minor
  feature:
    regex: ^features?[/-]
    mode: ContinuousDeployment
    tag: 'beta'
  pull-request:
    regex: ^(pull|pull-requests|pr)[/-]
    mode: ContinuousDeployment
    tag: 'pr'
  hotfix:
    regex: ^hotfix[/-]
    mode: ContinuousDelivery
    tag: 'beta'
    increment: Patch
  release:
    regex: ^releases?[/-]
    mode: ContinuousDelivery
    tag: 'rc'
    increment: Patch
  support:
    regex: ^support[/-]
    mode: ContinuousDelivery
    tag: ""
    increment: Patch